specVersion: "v1"
serviceId: "amazon-clone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "amazon-clone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
